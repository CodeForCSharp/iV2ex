<Window
    x:Class="iV2EX.MainWindow"
    xmlns:local="using:iV2EX"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Name="RootGrid" PointerPressed="RootGrid_PointerPressed">
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid x:Name="AppTitleBar" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="LeftPaddingColumn" Width="0"/>
                <ColumnDefinition x:Name="IconColumn" Width="0"/>
                <ColumnDefinition x:Name="TitleColumn" Width="Auto"/>
                <ColumnDefinition x:Name="DragColumn" Width="*"/>
                <ColumnDefinition x:Name="RightPaddingColumn" Width="0"/>
            </Grid.ColumnDefinitions>
            <Button
               x:Name="AppTitleBarBackButton"
               Grid.Column="1"
               Width="40"
               Height="32"
               Margin="8,0,0,0"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               Visibility="Collapsed"
               Background="Transparent">
                <FontIcon FontSize="14" Glyph="&#xE72B;" />
            </Button>
            <TextBlock x:Name="TitleTextBlock" 
               Text="iV2ex" 
               Style="{StaticResource CaptionTextBlockStyle}"
               Grid.Column="2"
               VerticalAlignment="Center"
               Margin="12,0,0,0"/>
        </Grid>
        <!-- Your pages will be loaded in this Frame -->
        <Frame Grid.Row="1" Height="Auto" x:Name="PageFrame" x:FieldModifier="public" />

    </Grid>

</Window>
